<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Transaction Status</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  <style>
    body {
      background-image: linear-gradient(120deg, #f6d365 0%, #fda085 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      font-family: Arial, sans-serif;
      margin: 0;
    }
    .message-container {
      text-align: center;
      padding: 40px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      background-color: rgba(255, 255, 255, 0.9);
      max-width: 80%;
    }
    h1 {
      color: #2c3e50;
      font-weight: bold;
      margin-bottom: 20px;
    }
    p {
      color: #555;
      margin-bottom: 10px;
    }
    .back-button {
      padding: 10px 20px;
      font-size: 16px;
      background-color:#dfcd92; /* Set button background to transparent */
      color: #fff; /* White button text color */
      border: none;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.3s, border-color 0.3s, color 0.3s; /* Apply transition effect */
      outline: none; /* Remove default button outline */
      border: 2px solid transparent; /* Hide button border */
    }
    .back-button:hover {
      background-color: rgba(246, 211, 101, 0.7); /* Adjusted color on hover */
      border-color: rgba(255, 255, 255, 0.5); /* Border color on hover */
    }
    .back-button:focus {
      background-color: rgba(246, 211, 101, 0.9); /* Adjusted color on focus */
      border-color: rgba(255, 255, 255, 0.7); /* Border color on focus */
    }
  </style>
</head>
<body>
  <div class="message-container">
    <h1>Thank you for submitting a testimonial.</h1>
    <p>Wait while we send you back to your Profile</p>
    <p>hahah</p>
    
    <!-- Button section -->
    <button onclick="goBack()" class="back-button">Go Back</button>
  </div>

  <!-- Bootstrap JS and dependencies (optional) -->
  <script>
    function goBack() {
      window.history.back();
    }
  </script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js" integrity="sha384-SsKJiEe/1+kz9O9R1dGom5LKLTV0R6oBk/iJrF+4hXaGpG3XjECD5uG5q7XyDdiH" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
</body>
</html>
