<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>CONFIRM TESTIMONIALS</title>
    <style>
      body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f3f6f8; /* Light blue-gray background */
            color: #333; /* Dark text color */
        }

        h2 {
            text-align: center;
            margin-top: 20px;
            color: #0080ff; /* Bright blue heading color */
        }

        p {
            text-align: center;
            color: #666; /* Grayish paragraph color */
        }

        table {
            width: 75%;
            margin: 20px auto;
            border-collapse: collapse;
            border: 1px solid #ccc; /* Light gray border */
            background-color: #fff; /* White table background */
        }

        th, td {
            border: 1px solid #ccc;
            padding: 10px;
            text-align: left;
        }

        th {
            background-color: #0080ff; /* Bright blue heading background */
            color: #fff; /* White text color */
        }

        tr:nth-child(even) {
            background-color: #f0f7fa; /* Light blue-gray alternate row background */
        }

        form {
            text-align: center;
            margin-top: 20px;
        }

        /* Back button styles */
        .back-button {
            display: inline-block;
            padding: 12px 24px;
            font-size: 16px;
            background-color: #003acc; /* Vivid green button color */
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
            transition: background-color 0.3s;
        }

        .top-right-button {
            position: absolute;
            top: 20px;
            right: 20px;
            padding: 12px 24px;
            font-size: 16px;
            background-color: #2980b9; /* Red button color */
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
            transition: background-color 0.3s;
        }

        .top-right-button:hover {
            background-color: #4db8ff; /* Darker red on hover */
        }
        
        /* Updated styles for table buttons */
        td {
            text-align: center;
            vertical-align: middle;
        }

        .change-availability-button {
            padding: 10px 20px;
            font-size: 14px;
            background-color: #3498db; /* Blue button color */
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
            transition: background-color 0.3s;
            display: block;
            margin: 0 auto;
        }
        .change-availability-button:hover {
            background-color: #2980b9; /* Darker blue on hover */
        }
    </style>
</head>
<body>
     <a href="/TheCarSpa/Login/Manager/SetAvailability/Change" class="top-right-button">Go to Specific Page</a>

    <h2>COnfirm Testimonials</h2>
    <form action="/TheCarSpa/Login/Manager/SetAvailability/Change" method="POST">
        <table>
            <thead>
                <tr>
                    <th>Team ID</th>
                    <th>Availablility</th>
                    <th>Set Availability</th>
                </tr>
            </thead>
            <tbody>
                <% users.forEach(function(user){ %>
                    <tr>
                        <td><%= user.TeamId %></td>
                        <td><%= user.TeamName %></td>
                        <td><%= user.availability %></td>


                             
                    <td>
                        <form action="/TheCarSpa/Login/Manager/SetAvailability/Change" method="post"> <!-- Replace with your actual route -->
                           
                            <input type="hidden" name="TeamId" value="<%= user.TeamId%>">
                            <input type="hidden" name="availability" value="<%= user.availability%>">
                            <button type="submit" class="btn btn-primary">Change Availability</button>
                        </form>
                    </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </form>
    <div style="text-align: center; margin-top: 20px;">
        <a href="/TheCarSpa/Login" style="text-decoration: none;">
            <button style="padding: 10px 20px; font-size: 16px; background-color: #3498db; color: #fff; border: none; border-radius: 4px; cursor: pointer;">Back to Main Page</button>
        </a>
    </div>

    <h3 style="text-align: center;">WHAT THIS PAGE DOES</h2>

    <!-- Description text -->
    <p style="text-align: center; font-size: 18px; color: #666; margin-bottom: 30px;">
        This page allows you to confirm testimonials for the Car Wash Manager Interface. Review the available testimonials and change their availability status as needed.
    </p>

</body>
</html>
